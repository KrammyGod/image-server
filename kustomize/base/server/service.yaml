apiVersion: v1
kind: Service
metadata:
  name: image-server-service
spec:
  type: ClusterIP
  selector:
    app: image-server
  ports:
    - name: rest
      port: 5000
      targetPort: rest
      protocol: TCP
