apiVersion: v1
kind: Service
metadata:
  name: nginx-static-service
spec:
  type: ClusterIP
  selector:
    app: nginx-static
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
    - name: http-lan
      port: 8090
      targetPort: http-lan
      protocol: TCP
